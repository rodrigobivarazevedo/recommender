# Web framework
fastapi==0.116.1
uvicorn==0.35.0
python-multipart==0.0.20
jinja2==3.1.6

# Data processing
numpy==2.2.6
pandas==2.3.1
scikit-learn==1.7.0

# Note: sentence-transformers excluded for production
# The embedding model is not loaded in production to reduce image size and startup time
# Only pre-computed embeddings are used for recommendations
